import styled  from "styled-components";

export const AppMusicItem = styled.div`
    border-radius: 10px;
    -webkit-backdrop-filter: blur(10px);
    backdrop-filter: blur(10px);
    background-color: rgba(255, 255, 255, 0.2);
    padding: 20px 10px;
`